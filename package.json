{
  "name": "monorepo-typescript",
  "version": "2.0.1",
  "main": "packages/dist/index.js",
  "types": "packages/dist/index.d.ts",
  "license": "ISC",
  "author": "EugeneKvach",
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/euhoo/monorepo.git"
  },
  "scripts": {
    "build:app": "npm run build --prefix packages/app",
    "build:first": "npm run build --prefix packages/first",
    "build:second": "npm run build --prefix packages/second",
    "build:all": "npm run build:app & npm run build:first & npm run build:second"
  },
  "dependencies": {
    "typescript": "^4.1.3"
  },
  "devDependencies": {}
}
