{
  "name": "monorepo-typescript",
  "version": "2.0.3",
  "main": "packages/dist/index.js",
  "types": "packages/dist/index.d.ts",
  "license": "ISC",
  "author": "EugeneKvach",
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/euhoo/monorepo-typescript.git"
  },
  "scripts": {
    "build:app": "npm run build --prefix packages/app",
    "build:types": "npm run build --prefix packages/types",
    "build:second": "npm run build --prefix packages/second",
    "build:all": "npm run build:app & npm run build:types & npm run build:second"
  },
  "dependencies": {
    "typescript": "^4.1.3"
  },
  "devDependencies": {}
}
